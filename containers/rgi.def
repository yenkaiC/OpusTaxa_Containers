Bootstrap: docker
From: condaforge/miniforge3:latest

%labels
    Author OpusTaxa
    Tool rgi
    Version 6.0.5

%post
    apt-get update && apt-get install -y procps wget && apt-get clean -y
    mamba install -y -c conda-forge -c bioconda \
        python=3.10 \
        rgi=6.0.5
    mamba clean -a -y

%environment
    export PATH=/opt/conda/bin:$PATH

%runscript
    exec "$@"
